package joelbits.model.ast.types;

public enum StatementType {
        ASSERT, BLOCK, BREAK, CASE, CATCH, CONTINUE, DO, EMPTY, EXPRESSION,
        FOR, IF, LABEL, OTHER, RETURN, SWITCH, SYNCHRONIZED, THROW, TRY,
        TYPEDECLARATION, WHILE
    }
